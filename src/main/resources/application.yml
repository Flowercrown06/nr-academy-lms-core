spring:
  application:
    name: nr-academy-lms-core

  datasource:
    url: jdbc:postgresql://localhost:5432/nr_academy_lms
    username: postgres
    password: password123  # Docker-d?ki ?ifr? il? eyni olmal?d?r
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080